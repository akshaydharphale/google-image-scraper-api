# Comma-separated list of accepted API keys. Leave empty to disable auth (dev mode).
API_KEYS=

# Seconds to wait for Google before returning a 503.
REQUEST_TIMEOUT=90

# Base URL used to build html_url / json_url in search_metadata.
PUBLIC_BASE_URL=http://localhost:8000
